{
  "name": "ratchetlock",
  "version": "0.1.0",
  "description": "프롬프트 계약 래칫 CLI — promptfoo 위에 동결/실패누적/버전관리 레이어를 얹는다.",
  "bin": {
    "ratchetlock": "dist/cli.js"
  },
  "files": [
    "dist",
    "src/replay-provider.js"
  ],
  "type": "commonjs",
  "scripts": {
    "build": "tsc && cp src/replay-provider.js dist/replay-provider.js",
    "test": "node --test"
  },
  "dependencies": {
    "promptfoo": "^0.121.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  }
}
